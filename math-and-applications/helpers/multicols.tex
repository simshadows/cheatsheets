% Adds a line between multicols columns
\def\MulticolsReset{%
    \setlength{\columnseprule}{0.4pt}%
    \setlength{\columnsep}{40pt}%
}
\MulticolsReset

\newcommand{\MulticolsBreak}{\vfill\null\columnbreak} % For use in the multicols environment.
\newcommand{\MulticolsCleanEnd}{\vfill\null} % For use in the multicols environment.
\newcommand{\MulticolsPhantomPlaceholder}{\phantom{x}} % An arbitrary box to occupy a multicols column.

% There's a lot of vertical space directly after a multicols environment.
% This is my current workaround to reduce that space.
% TODO: If there's a better solution to this, fix all usage of this command!
\newcommand{\MulticolsReduceVspaceAfter}{\vspace*{-10mm}}

\newenvironment{MulticolsNoSepRule}[1]{%
    \setlength{\columnseprule}{0pt}%
    \setlength{\columnsep}{40pt}%
    \begin{multicols}{#1}%
}{%
    \MulticolsCleanEnd%
    \end{multicols}%
    \MulticolsReset%
}

